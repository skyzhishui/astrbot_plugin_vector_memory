# AstrBot 向量记忆插件 🧠

一个基于向量语义检索的长期记忆系统，让 AI 能够真正"记住"重要信息。

## ✨ 功能特点

- 🔍 **语义检索**: 使用向量相似度搜索，而非关键词匹配
- 💾 **自动记忆**: 自动从对话中提取重要信息并存储
- 🎯 **智能注入**: 在对话时自动检索相关记忆并注入上下文
- 📊 **分类管理**: 支持多种记忆类别（偏好、事实、事件等）
- ⭐ **重要性评分**: 每条记忆都有重要性权重
- 🔐 **记忆分层**: 支持公共记忆、用户专属记忆、秘密记忆三种可见性
- 👤 **身份映射**: 支持多账号关联到同一用户身份
- 🏷️ **所有者标识**: 每条记忆都记录所有者信息，方便追溯
- 🚀 **ChromaDB 驱动**: 使用 ChromaDB 实现高效的向量索引和检索

## 📦 安装

1. 将插件放入 `AstrBot/data/plugins/astrbot_plugin_vector_memory/` 目录
2. 安装依赖: `pip install chromadb`
3. 在 AstrBot 管理面板中配置 Embedding Provider ID
4. 重启 AstrBot 或使用 `/memory_init` 初始化

## ⚙️ 配置说明

在 `_conf_schema.json` 中配置：

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `embedding_provider_id` | Embedding Provider ID（必填） | "" |
| `auto_remember` | 是否自动记忆对话中的重要信息 | true |
| `memory_threshold` | 自动记忆的对话轮数阈值 | 5 |
| `top_k` | 检索时返回的记忆数量 | 5 |
| `user_identity_map` | 用户身份映射配置（列表格式） | [] |
| `masters` | 主人列表，拥有最高权限（可查看和删除所有记忆） | [] |

### 用户身份映射配置

`user_identity_map` 是一个字符串列表，每个元素格式为 `"规范用户ID=会话ID1,会话ID2"`。

**示例配置：**
```json
{
  "user_identity_map": [
    "user001=qq_12345,wx_abcde",
    "user002=qq_67890,wx_fghij"
  ],
  "masters": ["user001", "user002"]
}
```

上述配置表示：
- `qq_12345` 和 `wx_abcde` 都是 `user001` 的身份
- `qq_67890` 和 `wx_fghij` 都是 `user002` 的身份
- `user001` 和 `user002` 是主人，拥有最高权限

## 🔐 记忆可见性分层

| 可见性 | 说明 | 适用场景 |
|--------|------|----------|
| `public` | 公共记忆，所有用户可见 | 家庭成员信息、基本设定 |
| `private` | 用户专属记忆，仅该用户可见 | 个人生日、个人偏好 |
| `secret` | 秘密记忆，仅主人可见 | 敏感凭证、隐私信息 |

### 权限规则

- **主人**: 可以查看和删除所有记忆
- **普通用户**: 只能查看公共记忆和自己的私有记忆
- **公共记忆**: 只有主人可以删除
- **私有/秘密记忆**: 只有所有者可以删除

## 🎮 使用方法

### LLM 工具（对话中使用）

直接与 AI 对话，它会自动使用这些工具：

- **记住信息**: "记住我喜欢吃草莓蛋糕"
- **记住秘密**: "记住我的密码是xxx，这是秘密"
- **回忆信息**: "你还记得我喜欢什么吗？"
- **列出记忆**: "列出你记得的所有事情"
- **删除记忆**: "忘记那条关于xxx的记忆"
- **查看统计**: "你的记忆系统状态如何？"

### 命令

- `/memory_init` - 初始化记忆系统
- `/memory_test` - 测试记忆系统是否正常工作

## 🧪 示例对话

```
用户: 记住我下周三有个会议
AI: ✓ 已记住: 下周三有个会议 (ID: 1, 类别: task, 可见性: private)

用户: 记住我的家人有小明和小红，这是公共信息
AI: ✓ 已记住: 家人有小明和小红 (ID: 2, 类别: personal, 可见性: public)

用户: 记住我的API密钥是sk-xxx，这是秘密
AI: ✓ 已记住: API密钥是sk-xxx (ID: 3, 类别: secret, 可见性: secret)

用户: 我有什么重要的事情吗？
AI: 找到 2 条相关记忆:
1. [task] 下周三有个会议
   相关度: 0.85 | 可见性: private | 所有者: user001
2. [personal] 家人有小明和小红
   相关度: 0.72 | 可见性: public | 所有者: user001
```

## 📁 数据存储

记忆数据存储在 `/AstrBot/data/vector_memory/` 目录（ChromaDB 持久化存储）

## 🔧 技术架构

```
用户消息 → Embedding → ChromaDB 向量搜索 → 权限过滤 → 返回相关记忆
                                    ↓
                              注入到 LLM 上下文
```

### 为什么选择 ChromaDB？

- ✅ **无需额外部署** - 嵌入式运行，像 SQLite 一样简单
- ✅ **自动向量索引** - 使用 HNSW 算法，检索速度快
- ✅ **支持元数据过滤** - 可按类别、重要性、可见性等条件筛选
- ✅ **Python 原生** - API 简洁，易于集成

## 📝 记忆类别

| 类别 | 说明 | 示例 |
|------|------|------|
| `preference` | 用户偏好 | 喜欢吃草莓蛋糕 |
| `fact` | 重要事实 | 生日是3月15日 |
| `personal` | 个人信息 | 工作是程序员 |
| `event` | 重要事件 | 下周三有会议 |
| `task` | 任务承诺 | 答应帮忙写代码 |
| `secret` | 秘密信息 | API密钥、密码 |
| `general` | 通用记忆 | 其他信息 |

## 📋 更新日志

### v1.2.1 (2026-03-29)
- ✨ 新增：`list_memories` 和 `recall_memories` 工具现在显示记忆的所有者信息
- ✨ 新增：`remember` 工具支持 `secret` 类别
- 🐛 修复：记忆列表显示格式优化

### v1.2.0 (2026-03-28)
- ✨ 新增：记忆可见性分层 (public/private/secret)
- ✨ 新增：用户身份映射配置
- ✨ 新增：主人权限系统
- 🐛 修复：`delete_memory` 和 `get_stats` 缺少 `user_id` 参数的 bug
- 🧹 移除：多余的 `admin_ids` 配置

### v1.0.0
- 🎉 初始版本发布

## 🐱 关于

由 Neko 猫猫女仆为您打造~ 喵~ 🐱
